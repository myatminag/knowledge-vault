# Test Structure

Tests are grouped by scope:

- `unit/` for isolated module behavior.
- `integration/` for workflows that cross module boundaries.
- `fixtures/` for reusable test inputs and expected outputs.
